# Modul A — Autentikasi & Akun (Fase 1)

Model modul ini (`App\Models\Auth\*`). Model bisnis WAJIB extends `App\Models\BaseAuditableModel` (audit otomatis, ADR-011/012); model `riwayat_*` extends `App\Models\BaseSnapshotModel` (ADR-006).
