# Modul D — Presensi & Remunerasi (Fase 5)

Model modul ini (`App\Models\Presensi\*`). Model bisnis WAJIB extends `App\Models\BaseAuditableModel` (audit otomatis, ADR-011/012); model `riwayat_*` extends `App\Models\BaseSnapshotModel` (ADR-006).
