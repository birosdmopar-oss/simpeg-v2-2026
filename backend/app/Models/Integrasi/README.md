# Modul H — Integrasi & API Eksternal (Fase 6)

Model modul ini (`App\Models\Integrasi\*`). Model bisnis WAJIB extends `App\Models\BaseAuditableModel` (audit otomatis, ADR-011/012); model `riwayat_*` extends `App\Models\BaseSnapshotModel` (ADR-006).
