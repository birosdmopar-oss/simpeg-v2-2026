# Modul G — Master Data & Pengaturan (Fase 2)

Model modul ini (`App\Models\MasterData\*`). Model bisnis WAJIB extends `App\Models\BaseAuditableModel` (audit otomatis, ADR-011/012); model `riwayat_*` extends `App\Models\BaseSnapshotModel` (ADR-006).
