# Modul E — Halo Simpeg (Fase 8)

Model modul ini (`App\Models\HaloSimpeg\*`). Model bisnis WAJIB extends `App\Models\BaseAuditableModel` (audit otomatis, ADR-011/012); model `riwayat_*` extends `App\Models\BaseSnapshotModel` (ADR-006).
